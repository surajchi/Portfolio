// Removed scroll-driven parallax — was adding useScroll listeners on top of
// Lenis + particle animations causing heavy main-thread congestion
export default function MultiLayerParallax() {
  return null;
}
