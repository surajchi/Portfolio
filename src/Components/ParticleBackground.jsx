// Replaced with CSS-only particles — no framer-motion animation loops
// that were causing scroll jank with 35 simultaneously animated elements
export default function ParticleBackground() {
  return null;
}
